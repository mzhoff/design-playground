# packages/theme-compiler

Компилятор тем.

Будущие выходные форматы:

- CSS variables;
- Tailwind CSS 4 theme fragment;
- JSON preset для Playground;
- Token Studio-compatible JSON;
- migration report при изменении схемы токенов.
