# packages/tokens

Источник правды для базовых и семантических дизайн-токенов.

Пакет должен хранить контракт токенов, а не разрозненные CSS-значения. Генерация CSS, Tailwind и Token Studio payload относится к `@design-playground/theme-compiler`.
